sudo apt update
sudo apt install aria2

python package:
pip install aria2p
aria2p
aioaria2
direct JSON-RPC with httpx

===================================
Personally, I'd rather use:

systemctl start aria2
import subprocess

def run():
    subprocess.run(
        ["systemctl", "--user", "start", "aria2"],
        check=False
    )

    return {"status": True}

    If you're already running OpenClaw on a Linux server, I'd make aria2 a permanent service:

systemctl --user enable aria2
systemctl --user start aria2
hen remove aria2_start.py completely.
skill:
check health
↓
if unhealthy → tell user
↓
add download
↓
monitor progress

===========
mkdir -p ~/.config/aria2
touch ~/.config/aria2/aria2.session
nano ~/.config/aria2/aria2.conf

/home/wineo/.config
This is a standard Linux location for per-user application configuration files.


mkdir -p ~/.config/systemd/user
nano ~/.config/systemd/user/aria2.service
:
[Unit]
Description=Aria2 Download Manager
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/aria2c --conf-path=/home/wineo/.config/aria2/aria2.conf
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
----------------------
systemctl --user daemon-reload
systemctl --user enable aria2
systemctl --user start aria2
systemctl --user status aria2